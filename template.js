const firstName = "Justin";
const lastName = "Timberlake";
const fullName= firstName + ' ' + lastName;
const fullName2 = `${firstName} ${lastName} is a good boy`;

const multi = `Here I was
Standing right infornt
Just being there`
console.log(multi);